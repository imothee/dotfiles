# Per-host PRIVATE overlays — machine/company-specific, never publish.
local.conf
local.fish
tmux/local.conf
fish/local.fish

# tpm-managed plugins are provisioned per box, not vendored here.
tmux/plugins/
